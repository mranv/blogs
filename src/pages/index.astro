---
import { getCollection } from "astro:content";
import Layout from "@layouts/Layout.astro";
import Header from "@components/Header.astro";
import Footer from "@components/Footer.astro";
import Hr from "@components/Hr.astro";
import LazyCard from "@components/LazyCard.astro";
import Socials from "@components/Socials.astro";
import Chatbot from "@components/Chatbot";
import Globe from "@components/Globe";
import getSortedPosts from "@utils/getSortedPosts";
import { SOCIALS } from "@config";

const posts = await getCollection("blog");
const sortedPosts = getSortedPosts(posts);
const featuredPosts = sortedPosts.filter(({ data }) => data.featured);
const recentPosts = sortedPosts.filter(({ data }) => !data.featured);
const socialCount = SOCIALS.filter(social => social.active).length;

// List of items to search in for chatbot
const searchList = sortedPosts.map(({ data, slug }) => ({
  title: data.title,
  description: data.description,
  data,
  slug,
}));

// Extract unique tags from all posts for filter section
const allTags = [...new Set(sortedPosts.flatMap(post => post.data.tags || []))];

// Featured technologies/skills
const featuredTechnologies = [
  {
    name: "Kubernetes",
    icon: "🚢",
    description: "Container orchestration and management",
  },
  {
    name: "eBPF",
    icon: "🔬",
    description: "Kernel-level observability and security",
  },
  {
    name: "Rust",
    icon: "🦀",
    description: "Systems programming and performance",
  },
  {
    name: "DevSecOps",
    icon: "🔒",
    description: "Security-integrated development practices",
  },
  {
    name: "Cloud Security",
    icon: "☁️",
    description: "Multi-cloud security architecture",
  },
  {
    name: "Terraform",
    icon: "🏗️",
    description: "Infrastructure as Code automation",
  },
];
---

<Layout>
  <Header />

  <!-- Authentication Check -->
  <script>
    // Check if user is authenticated
    if (localStorage.getItem("isAuthenticated") !== "true") {
      window.location.href = "/login";
    }
  </script>

  <!-- Loading State -->
  <div id="loadingState" class="loading-state">
    <div class="text-center">
      <div class="loading-spinner"></div>
      <p class="loading-text">Loading...</p>
    </div>
  </div>

  <script>
    // Hide loading state once page is loaded
    window.addEventListener("load", () => {
      const loadingState = document.getElementById("loadingState");
      if (loadingState) {
        loadingState.style.display = "none";
      }
    });
  </script>

  <main
    id="main-content"
    class="w-screen bg-background text-foreground claude-bg-primary claude-text-primary"
  >
    <!-- Hero Section - Full Width -->
    <section
      id="hero"
      class="w-screen py-16 claude-bg-primary"
      aria-labelledby="hero-heading"
    >
      <div class="container max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <h1
          id="hero-heading"
          class="mb-6 text-center text-4xl sm:text-5xl lg:text-6xl font-bold text-reveal gradient-text-primary animate-gradient-text"
        >
          Anubhav Gain
        </h1>
        <h2
          class="mb-4 text-center text-xl sm:text-2xl font-semibold text-muted-foreground animate-fade-in-up animate-delay-200"
        >
          DevSecOps Engineer &amp; Cybersecurity Expert
        </h2>
        <p
          class="mb-8 text-center text-lg sm:text-xl text-muted-foreground/90 animate-fade-in-up animate-delay-400"
        >
          Dive beneath the surface of technology—where real innovation happens.
          Exploring the depths of security, automation, and modern
          infrastructure.
        </p>

        <!-- Interactive Globe Section -->
        <div
          class="globe-container relative flex flex-col items-center mt-12 mb-8"
        >
          <div class="mb-8 text-center">
            <h3
              class="text-lg font-semibold text-primary mb-2 animate-fade-in-up animate-delay-600"
            >
              Global Cybersecurity Network
            </h3>
            <p
              class="text-sm text-muted-foreground animate-fade-in-up animate-delay-700"
            >
              Explore key technology hubs and cybersecurity centers worldwide
            </p>
          </div>

          <div class="w-full max-w-lg mx-auto mb-8">
            <Globe
              client:load
              size={400}
              className="mx-auto"
              autoRotate={true}
              rotationSpeed={0.008}
              markers={[
                { location: [37.7749, -122.4194], size: 0.12 }, // San Francisco
                { location: [40.7128, -74.006], size: 0.1 }, // New York
                { location: [51.5074, -0.1278], size: 0.08 }, // London
                { location: [35.6762, 139.6503], size: 0.07 }, // Tokyo
                { location: [52.52, 13.405], size: 0.06 }, // Berlin
                { location: [55.7558, 37.6176], size: 0.05 }, // Moscow
                { location: [39.9042, 116.4074], size: 0.06 }, // Beijing
                { location: [19.076, 72.8777], size: 0.05 }, // Mumbai
                { location: [1.3521, 103.8198], size: 0.05 }, // Singapore
                { location: [-33.8688, 151.2093], size: 0.04 }, // Sydney
                { location: [59.3293, 18.0686], size: 0.04 }, // Stockholm
                { location: [45.5017, -73.5673], size: 0.04 }, // Montreal
              ]}
            />
          </div>

          <div
            class="grid grid-cols-2 md:grid-cols-4 gap-4 w-full max-w-2xl mx-auto"
          >
            <div
              class="flex flex-col items-center text-center p-3 rounded-lg bg-card/50 border border-border/30"
            >
              <div class="text-2xl mb-2">🌐</div>
              <div class="text-sm font-medium">Global Reach</div>
            </div>
            <div
              class="flex flex-col items-center text-center p-3 rounded-lg bg-card/50 border border-border/30"
            >
              <div class="text-2xl mb-2">🔒</div>
              <div class="text-sm font-medium">Security Focus</div>
            </div>
            <div
              class="flex flex-col items-center text-center p-3 rounded-lg bg-card/50 border border-border/30"
            >
              <div class="text-2xl mb-2">⚡</div>
              <div class="text-sm font-medium">Real-time Monitoring</div>
            </div>
            <div
              class="flex flex-col items-center text-center p-3 rounded-lg bg-card/50 border border-border/30"
            >
              <div class="text-2xl mb-2">🛡️</div>
              <div class="text-sm font-medium">Threat Detection</div>
            </div>
          </div>
        </div>

        <!-- Stats Section with theme-aware Cards -->
        <div class="mt-16 animate-fade-in-up animate-delay-800">
          <div
            class="grid grid-cols-2 md:grid-cols-4 gap-6 text-center max-w-4xl mx-auto"
          >
            <div
              class="p-6 rounded-lg bg-card/80 border border-border/30 backdrop-blur-sm hover:bg-card/90 transition-all duration-300 hover:scale-105 hover:shadow-lg"
            >
              <div class="text-3xl font-bold text-primary mb-2">
                {sortedPosts.length}+
              </div>
              <div class="text-sm text-muted-foreground">Articles</div>
            </div>
            <div
              class="p-6 rounded-lg bg-card/80 border border-border/30 backdrop-blur-sm hover:bg-card/90 transition-all duration-300 hover:scale-105 hover:shadow-lg"
            >
              <div class="text-3xl font-bold text-primary mb-2">10+</div>
              <div class="text-sm text-muted-foreground">Technologies</div>
            </div>
            <div
              class="p-6 rounded-lg bg-card/80 border border-border/30 backdrop-blur-sm hover:bg-card/90 transition-all duration-300 hover:scale-105 hover:shadow-lg"
            >
              <div class="text-3xl font-bold text-primary mb-2">5+</div>
              <div class="text-sm text-muted-foreground">Years Experience</div>
            </div>
            <div
              class="p-6 rounded-lg bg-card/80 border border-border/30 backdrop-blur-sm hover:bg-card/90 transition-all duration-300 hover:scale-105 hover:shadow-lg"
            >
              <div class="text-3xl font-bold text-primary mb-2">24/7</div>
              <div class="text-sm text-muted-foreground">Security Focus</div>
            </div>
          </div>
        </div>

        <p
          class="mt-12 text-center text-muted-foreground/90 animate-fade-in-up animate-delay-1000"
        >
          Welcome to my technical exploration space. Here you'll find in-depth
          articles on
          <span class="text-primary font-semibold">DevSecOps</span>,
          <span class="text-primary font-semibold">cybersecurity</span>,
          <span class="text-primary font-semibold">cloud security</span>, and
          <span class="text-primary font-semibold">modern infrastructure</span> practices.
        </p>

        {
          socialCount > 0 && (
            <div class="mt-8 flex flex-col items-center justify-center sm:flex-row animate-on-scroll">
              <div class="mb-2 mr-4 font-semibold sm:mb-0 hover-bounce text-foreground">
                Social Links:
              </div>
              <Socials />
            </div>
          )
        }
      </div>
    </section>

    <Hr />

    <!-- Featured Technologies Section - Full Width -->
    <section id="technologies" class="w-screen py-16 claude-bg-primary">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12">
          <h2 class="text-3xl sm:text-4xl font-bold text-foreground mb-4">
            Core Technologies
          </h2>
          <p class="text-lg text-muted-foreground max-w-2xl mx-auto">
            The cutting-edge tools and technologies I work with to build secure,
            scalable solutions
          </p>
        </div>
        <div
          class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-6 gap-4 max-w-6xl mx-auto"
        >
          {
            featuredTechnologies.map((tech, _index) => (
              <div class="group p-4 rounded-lg bg-card/80 border border-border/30 backdrop-blur-sm hover:bg-card/90 transition-all duration-300 hover:scale-105 hover:shadow-lg">
                <div class="flex flex-col items-center text-center space-y-3">
                  <span class="text-3xl group-hover:scale-110 transition-transform duration-300">
                    {tech.icon}
                  </span>
                  <h3 class="font-semibold text-foreground">{tech.name}</h3>
                  <p class="text-xs text-muted-foreground leading-relaxed">
                    {tech.description}
                  </p>
                </div>
              </div>
            ))
          }
        </div>
      </div>
    </section>

    <Hr />

    {
      featuredPosts.length > 0 && (
        <section id="featured" class="w-screen py-16 claude-bg-primary">
          <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-12">
              <h2 class="text-3xl sm:text-4xl font-bold text-foreground mb-4">
                Featured Articles
              </h2>
              <p class="text-lg text-muted-foreground max-w-2xl mx-auto">
                Handpicked articles showcasing cutting-edge security practices
                and innovative solutions
              </p>
            </div>
            <ul class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 max-w-6xl mx-auto">
              {featuredPosts.map(({ data, slug }, _index) => (
                <LazyCard
                  href={`/posts/${slug}/`}
                  frontmatter={data}
                  secHeading={false}
                  index={_index}
                />
              ))}
            </ul>
          </div>
        </section>
      )
    }

    {recentPosts.length > 0 && <Hr />}

    {
      recentPosts.length > 0 && (
        <section id="recent-posts" class="w-screen py-16 claude-bg-primary">
          <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-12">
              <h2 class="text-3xl sm:text-4xl font-bold text-foreground mb-4">
                Latest Insights
              </h2>
              <p class="text-lg text-muted-foreground max-w-2xl mx-auto">
                Fresh perspectives on emerging security challenges and
                technology trends
              </p>
            </div>
            <div class="text-center mb-12">
              <h3 class="text-lg font-semibold text-primary mb-6">
                Explore by Topic
              </h3>
              <div class="flex flex-wrap justify-center gap-2 max-w-4xl mx-auto">
                {allTags.slice(0, 12).map(tag => (
                  <span class="inline-flex items-center px-3 py-1.5 rounded-full text-xs font-medium bg-primary/10 text-primary border border-primary/20 hover:bg-primary/20 transition-colors cursor-pointer">
                    {tag}
                  </span>
                ))}
              </div>
            </div>

            <ul class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 max-w-6xl mx-auto">
              {recentPosts.slice(0, 8).map(({ data, slug }, _index) => (
                <LazyCard
                  href={`/posts/${slug}/`}
                  frontmatter={data}
                  secHeading={false}
                  index={_index + featuredPosts.length}
                />
              ))}
            </ul>
          </div>
        </section>
      )
    }

    <div class="py-12 text-center claude-bg-primary w-screen">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <a
          href="/posts/"
          class="inline-flex items-center px-6 py-3 rounded-lg bg-primary text-primary-foreground font-medium hover:bg-primary/90 transition-all duration-200 hover:scale-105 hover:shadow-lg"
        >
          All Posts
          <svg
            xmlns="http://www.w3.org/2000/svg"
            class="ml-2 h-5 w-5"
            viewBox="0 0 20 20"
            fill="currentColor"
          >
            <path
              fill-rule="evenodd"
              d="M10.293 5.293a1 1 0 011.414 0l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414-1.414L12.586 11H5a1 1 0 110-2h7.586l-2.293-2.293a1 1 0 010-1.414z"
              clip-rule="evenodd"></path>
          </svg>
        </a>
      </div>
    </div>

    <Hr />

    <!-- Newsletter Signup Section - Full Width -->
    <section id="newsletter" class="w-screen py-16 claude-bg-primary">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center">
          <div
            class="p-8 rounded-xl bg-card/80 border border-border/30 backdrop-blur-sm"
          >
            <div class="mb-8">
              <h3 class="text-2xl font-semibold text-primary mb-4">
                Stay Updated
              </h3>
              <p class="text-muted-foreground max-w-2xl mx-auto">
                Get the latest insights on cybersecurity, DevSecOps, and
                cutting-edge technology delivered to your inbox.
              </p>
            </div>
            <div class="flex flex-col sm:flex-row gap-4 max-w-md mx-auto">
              <input
                type="email"
                placeholder="Enter your email"
                class="flex-1 px-4 py-3 rounded-lg border border-border bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent"
              />
              <button
                type="submit"
                class="px-6 py-3 rounded-lg bg-primary text-primary-foreground font-medium hover:bg-primary/90 transition-all duration-200 hover:scale-105 sm:w-auto whitespace-nowrap"
              >
                Subscribe
              </button>
            </div>
            <p class="text-xs text-muted-foreground mt-4">
              No spam, unsubscribe at any time. Your privacy is important to us.
            </p>
          </div>
        </div>
      </div>
    </section>
  </main>

  <Footer />
  <Chatbot client:load searchList={searchList} />
</Layout>

<script>
  // Defer interaction setup to avoid blocking main thread
  if ("requestIdleCallback" in window) {
    requestIdleCallback(() => {
      setupGlobeEnhancements();
      setupNewsletterForm();
      setupCategoryFilters();
    });
  } else {
    // Fallback for browsers without requestIdleCallback
    setTimeout(() => {
      setupGlobeEnhancements();
      setupNewsletterForm();
      setupCategoryFilters();
    }, 1);
  }

  function setupGlobeEnhancements() {
    // Add theme-aware globe updates
    const themeObserver = new MutationObserver(mutations => {
      mutations.forEach(mutation => {
        if (
          mutation.type === "attributes" &&
          mutation.attributeName === "data-theme"
        ) {
          const theme = document.documentElement.getAttribute("data-theme");
          // Globe component will automatically update via React props
          console.log("Theme changed to:", theme);
        }
      });
    });

    themeObserver.observe(document.documentElement, {
      attributes: true,
      attributeFilter: ["data-theme"],
    });

    // Add globe container animations on scroll
    const globeContainer = document.querySelector(".globe-container");
    if (globeContainer) {
      const observer = new IntersectionObserver(
        entries => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              entry.target.classList.add("animate-in");
            }
          });
        },
        { threshold: 0.3 }
      );

      observer.observe(globeContainer);
    }
  }

  function setupNewsletterForm() {
    const newsletterForm = document.querySelector(
      '#newsletter form, #newsletter button[type="submit"]'
    );
    if (newsletterForm) {
      newsletterForm.addEventListener("click", function (e) {
        e.preventDefault();
        const emailInput = document.querySelector(
          '#newsletter input[type="email"]'
        ) as HTMLInputElement;
        if (emailInput && emailInput.value) {
          // Simple email validation
          const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
          if (emailRegex.test(emailInput.value)) {
            // In a real implementation, you would send this to your backend
            alert("Thanks for subscribing! We'll send you updates soon.");
            emailInput.value = "";
          } else {
            alert("Please enter a valid email address.");
          }
        } else {
          alert("Please enter your email address.");
        }
      });
    }
  }

  function setupCategoryFilters() {
    const categoryBadges = document.querySelectorAll(
      '#recent-posts .inline-flex[class*="cursor-pointer"]'
    );

    categoryBadges.forEach(badge => {
      badge.addEventListener("click", function (this: HTMLElement) {
        const tag = this.textContent?.trim();
        if (tag) {
          // Navigate to posts page with tag filter
          window.location.href = `/tags/${tag.toLowerCase().replace(/\s+/g, "-")}/`;
        }
      });
    });
  }

  // Enhanced accessibility
  document.addEventListener("DOMContentLoaded", function () {
    // Add ARIA labels for interactive elements
    const interactiveElements = document.querySelectorAll(
      '[class*="cursor-pointer"], button, a'
    );
    interactiveElements.forEach(element => {
      if (
        !element.getAttribute("aria-label") &&
        !element.getAttribute("aria-labelledby")
      ) {
        const text = element.textContent?.trim();
        if (text) {
          element.setAttribute("aria-label", text);
        }
      }
    });

    // Keyboard navigation for category badges
    const categoryBadges = document.querySelectorAll(
      '#recent-posts .inline-flex[class*="cursor-pointer"]'
    );
    categoryBadges.forEach(badge => {
      badge.setAttribute("tabindex", "0");
      badge.setAttribute("role", "button");

      badge.addEventListener("keydown", function (e) {
        const keyboardEvent = e as KeyboardEvent;
        if (keyboardEvent.key === "Enter" || keyboardEvent.key === " ") {
          e.preventDefault();
          (badge as HTMLElement).click();
        }
      });
    });
  });
</script>
